"Prog 3 Practice - Hello World"
"If empty repository then it will happen simply(git branch -M main,git push -u origin main). If repository content some file then another rules.(git branch -M main,git pull origin main --allow-unrelated-histories,git push -u origin main)"
"Now i am changing in github. need to check it will update in local file or not. Priviously i will change in local file by using cmd it will auto matically update in github"
